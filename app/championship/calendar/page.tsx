import{createClient}from"@/lib/supabase/server";export default async function P(){const s=await createClient();const{data,error}=await s.from("rounds").select("*").order("event_date");return <><h1>Championship Calendar</h1>{error?<p>{error.message}</p>:<div className="grid">{(data||[]).map((r:any)=><div className="card" key={r.id}><b>{r.name}</b><p className="muted">{r.event_date} · {r.venue}</p></div>)}</div>}</>}
