"use client";import{useState}from"react";import{rounds}from"@/lib/sample";export default function P(){const[a,setA]=useState<string[]>(["r1"]);return <><h1 className="text-3xl font-bold">My Availability</h1><div className="card p-6 mt-5 space-y-3">{rounds.map(r=><label className="block" key={r.id}><input className="mr-2" type="checkbox" checked={a.includes(r.id)} onChange={()=>setA(x=>x.includes(r.id)?x.filter(y=>y!==r.id):[...x,r.id])}/>{r.name} — {r.date}</label>)}<select className="input"><option>Looking for a team</option><option>Available as reserve</option><option>Already in a team</option><option>Unavailable</option></select><button className="btn">Save Availability</button></div></>}