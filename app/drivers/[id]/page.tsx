import{drivers,rounds}from"@/lib/sample";import Link from"next/link";export default async function P({params}:{params:Promise<{id:string}>}){const{id}=await params;const d=drivers.find(x=>x.id===id);if(!d)return <p>Driver not found.</p>;return <div className="max-w-3xl"><Link className="text-red-300" href="/drivers">← Back</Link><div className="card p-7 mt-4"><h1 className="text-3xl font-bold">{d.name}</h1><p className="muted">{d.club} · {d.experience}</p><p className="mt-5">{d.bio}</p><h2 className="text-xl font-bold mt-6">Round Availability</h2><div className="space-y-2 mt-3">{rounds.map(r=><div className="card p-3 flex justify-between" key={r.id}><span>{r.name}</span><span>{d.rounds.includes(r.id)?"🟢 Available":"⚪ Unavailable"}</span></div>)}</div><button className="btn mt-5">Send Contact Request</button></div></div>}