"use client";import{useState}from"react";import{createClient}from"@/lib/supabase/client";export default function P(){const[m,setM]=useState("");async function login(f:FormData){const{error}=await createClient().auth.signInWithPassword({email:String(f.get("email")),password:String(f.get("password"))});if(error)setM(error.message);else location.href="/drivers"}return <><h1>Login</h1><form action={login} className="card grid" style={{maxWidth:500}}><input className="input" name="email" type="email" placeholder="Email"/><input className="input" name="password" type="password" placeholder="Password"/><button className="btn">Login</button>{m&&<p>{m}</p>}</form></>}
