"use client";import {useState}from"react";import{createClient}from"@/lib/supabase/client";export default function P(){const[m,setM]=useState("");async function go(f:FormData){const{error}=await createClient().auth.signInWithPassword({email:String(f.get("email")),password:String(f.get("password"))});if(error)setM(error.message);else location.href="/driver-area"}return <><h1>Login</h1><form action={go} className="card space" style={{maxWidth:520}}><div className="grid"><input className="input" required name="email" type="email" placeholder="Email"/><input className="input" required name="password" type="password" placeholder="Password"/><button className="btn">Login</button>{m&&<p>{m}</p>}</div></form></>}