"use client";import{useState}from"react";import{createClient}from"@/lib/supabase/client";export default function P(){const[m,setM]=useState("");async function submit(f:FormData){const name=String(f.get("name")||""),email=String(f.get("email")||"");const{data,error}=await createClient().auth.signUp({email,password:String(f.get("password")||""),options:{data:{name,club:String(f.get("club")||""),role:"driver"}}});if(error){setM(error.message);return}if(!data.session){setM("Account created. Confirm your email, then log in.");return}const classes=String(f.get("classes")||"").split(",").map(x=>x.trim()).filter(Boolean);const{error:e}=await createClient().from("drivers").upsert({profile_id:data.user!.id,classes,experience:String(f.get("experience")||""),bio:String(f.get("bio")||""),endurance_experience:String(f.get("endurance")||"")});setM(e?e.message:"Registration complete.")}return <><h1>Driver Registration</h1><form action={submit} className="card grid" style={{maxWidth:650}}><input required className="input" name="name" placeholder="Full name"/><input required className="input" name="email" type="email" placeholder="Email"/><input required className="input" name="password" type="password" minLength={6} placeholder="Password"/><input className="input" name="club" placeholder="RC club"/><select className="input" name="experience"><option>Beginner</option><option>Intermediate</option><option>Advanced</option><option>Expert</option></select><input className="input" name="classes" placeholder="Classes, separated by commas"/><input className="input" name="endurance" placeholder="Endurance experience"/><textarea className="input" name="bio" placeholder="Biography"/><button className="btn">Create Driver Profile</button>{m&&<p className="muted">{m}</p>}</form></>}
